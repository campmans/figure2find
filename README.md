# figure2find

# Update conda to the newest version

conda update -n base -c defaults conda

# Create an environment, for example with conda

conda create -n f2f
conda activate f2f
conda install pip

# Install the requirements

pip install -r requirements.txt